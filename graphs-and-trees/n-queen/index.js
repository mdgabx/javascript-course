function dfsNQueens(n) {
  if(n < 1) return [];

  let solutions = [];
  let cols = new Set();
  let diag1 = new Set();
  let diag2 = new Set(); 
  let placement = new Array(n).fill(-1);

  function backtrack(row) {
    
  }
}

dfsNQueens(1);